export default function(event) {
  let minLetters = 2
  let searchURL = event.target

  if (event.target.value > minLetters) {
  }
// $.post(searchURL, {
//   s: query.term,
//   resultsPerPage: 30
// }, null, 'json').then(function (resp) {
//   response(resp.products)
// }).fail(response)
}
