export default function(e) {

  // $.post(searchURL, {
  //   s: query.term,
  //   resultsPerPage: 30
  // }, null, 'json').then(function (resp) {
  //   response(resp.products)
  // }).fail(response)
}
